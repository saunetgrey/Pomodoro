export const escape = value => String(value).replace(/[&<>"']/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[char]));
const shell = (title, content) => `<!doctype html><html><body style="margin:0;background:#f7f6f2;font-family:Arial,sans-serif;color:#28382e"><table role="presentation" width="100%"><tr><td align="center" style="padding:32px 16px"><table role="presentation" width="100%" style="max-width:600px;background:white;border-radius:16px"><tr><td style="padding:32px"><p style="color:#71816e;font-size:12px;letter-spacing:2px">MORNING TASKS</p><h1 style="font-family:Georgia,serif;font-weight:normal">${escape(title)}</h1>${content}<p style="margin-top:32px;color:#808577;font-size:12px">Make time for what matters.</p></td></tr></table></td></tr></table></body></html>`;
export function dailyEmail(tasks, baseUrl) {
  return shell('Your day, planned.', `<p>${tasks.length} tasks · ${tasks.reduce((sum, task) => sum + task.minutes, 0)} minutes of focus.</p>` + tasks.map(task => `<div style="border:1px solid #dfe3d9;border-radius:10px;padding:20px;margin-top:16px"><h2 style="font-size:18px">${escape(task.task)}</h2><p style="color:#71816e">${task.minutes} minutes</p><a href="${escape(baseUrl + '/?task=' + encodeURIComponent(task.id))}" style="display:inline-block;background:#344e37;color:white;text-decoration:none;padding:12px 20px;border-radius:6px">Open task timer</a></div>`).join('') + '<p style="font-size:12px;color:#71816e">Open a task, then press Start timer on the page.</p>');
}
export function completedEmail(run) {
  return shell('Task completed!', `<div style="padding:24px;background:#eaf0e3;border-radius:10px"><h2 style="font-size:20px">${escape(run.task)}</h2><p>Your ${run.minutes}-minute timer is complete. Well done!</p></div>`);
}
